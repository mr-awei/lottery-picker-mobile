org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.internal.http.connectionTimeout=120000
org.gradle.internal.http.socketTimeout=120000
org.gradle.java.home=C:\\Tools\\jdk21\\jdk-21.0.12.1+1
android.useAndroidX=true

# === Release signing ===
# Keystore file (gitignored) lives at android/app/lottery-release-key-v2.jks
# （2026-08-24 重新生成：旧 lottery-release-key.jks 的密码未留存，无法恢复）
# 密码同时写入本文件（本地个人项目），build.gradle 的 hasProperty 分支会读取；
# 若开源请把下面两行密码移到环境变量 ORG_GRADLE_PROJECT_* 后再提交。
MYAPP_RELEASE_STORE_FILE=lottery-release-key-v2.jks
MYAPP_RELEASE_KEY_ALIAS=lotterykey
MYAPP_RELEASE_STORE_PASSWORD=lottery2026
MYAPP_RELEASE_KEY_PASSWORD=lottery2026
